apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ parameters.name }}  # Use the template parameter here
  description: A Chainlit chatbot using Google Gemini via LangChain.
  tags:
    - chainlit
    - gemini
    - langchain
    - python
spec:
  type: service
  owner: user:guest
  lifecycle: experimental
